[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "resonant-medium-project"
version = "0.0.0"
description = "Resonant Medium Project — evidence pack snapshot"
requires-python = ">=3.10"
